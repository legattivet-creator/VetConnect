<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Para arquivos no armazenamento externo privado do app -->
    <external-files-path name="shared_files" path="." />
    <!-- Para arquivos no cache externo privado do app -->
    <external-cache-path name="shared_cache" path="." />
    <!-- Para arquivos no cache interno privado do app -->
    <cache-path name="internal_cache" path="." />
    <!-- Para arquivos no armazenamento interno privado do app -->
    <files-path name="internal_files" path="." />
</paths>
